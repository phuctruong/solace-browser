# /northstar - Load Mission Context

Read `NORTHSTAR.md` and `specs/BUILD-SPEC.md`, then summarize:
1. Current phase and goal
2. What has been built vs what still needs building
3. The next concrete action

Also check `.claude/memory/context.md` if it exists.

Output a brief session brief:
- **Phase**: what phase we're in
- **Built**: what already exists (check for ui_server.py, recipes/linkedin-*.recipe.json)
- **Next**: the immediate next task

Keep it under 10 lines. This is a session orientation, not a full report.
